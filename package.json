{
  "name": "droolcat-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A visual cockpit on top of the Claude Code CLI — drives it headless and renders agents, actions, code, and windows as live, navigable graphs.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "app": "tauri dev",
    "app:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource-variable/jetbrains-mono": "^5.2.8",
    "@tauri-apps/api": "^2.1.1"
  }
}
